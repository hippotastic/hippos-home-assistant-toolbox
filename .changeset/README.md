# Changesets

Add a changeset for every user-visible change that should appear in release
notes. Maintenance-only changes do not require one.

Each changeset selects a semantic version bump and contains concise Markdown
for the generated changelog and GitHub release. The release workflow keeps the
root package version and Home Assistant integration manifest synchronized.
