{
  "config": {
    "abort": {
      "already_configured": "Hippo's Home Assistant Toolbox is already configured."
    },
    "step": {
      "user": {
        "description": "Install and keep the published blueprint collection up to date.",
        "title": "Set up Hippo's Home Assistant Toolbox"
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "release_channel": "Release channel"
        },
        "data_description": {
          "release_channel": "Stable receives published blueprint updates. Beta receives changes early for testing and may be less reliable."
        },
        "description": "Choose which blueprint catalog this Home Assistant instance follows.",
        "title": "Configure blueprint updates"
      }
    }
  },
  "entity": {
    "button": {
      "check_for_updates": {
        "name": "Check for updates"
      }
    },
    "update": {
      "blueprints": {
        "name": "Blueprints"
      }
    }
  },
  "issues": {
    "deprecated": {
      "description": "The blueprint **{blueprint_name}** is no longer maintained by this collection. Its local file remains available at `{path}` and will not be deleted or updated. {message}",
      "title": "Blueprint is deprecated"
    },
    "local_modified": {
      "description": "The local file for **{blueprint_name}** at `{path}` differs from the last managed version. It has not been overwritten. Use Repair to back it up and restore the published version, or keep your local version and ignore this issue.",
      "title": "Blueprint has local changes"
    }
  },
  "repairs": {
    "abort": {
      "integration_not_configured": "The integration is no longer configured."
    },
    "step": {
      "init": {
        "description": "The current file will be backed up, then blueprint `{blueprint_id}` will be replaced with the latest published version. This cannot merge local changes.",
        "title": "Restore published blueprint"
      }
    }
  }
}
