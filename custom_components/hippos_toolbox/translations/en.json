{
  "config": {
    "abort": {
      "already_configured": "Hippo's Home Assistant Toolbox is already configured."
    },
    "step": {
      "user": {
        "description": "Install and keep the published blueprint collection up to date.",
        "title": "Set up Hippo's Home Assistant Toolbox"
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "update_channel": "Update channel"
        },
        "data_description": {
          "update_channel": "Stable follows published releases. Development follows the latest blueprint changes on the main branch and may include changes that have not been released yet."
        },
        "description": "Choose which blueprint updates this Home Assistant instance follows.",
        "title": "Configure blueprint updates"
      }
    }
  },
  "entity": {
    "button": {
      "check_for_updates": {
        "name": "Check for updates"
      }
    },
    "update": {
      "blueprints": {
        "name": "Blueprints"
      }
    }
  },
  "issues": {
    "deprecated": {
      "description": "The blueprint **{blueprint_name}** is no longer maintained by this collection. Its local file remains available at `{path}` and will not be deleted or updated. {message}",
      "title": "Blueprint is deprecated"
    },
    "local_modified": {
      "fix_flow": {
        "abort": {
          "integration_not_configured": "The integration is no longer configured.",
          "issue_ignored": "The local file for **{blueprint_name}** was left unchanged, and this Repair has been ignored. The integration will not overwrite this file while it differs from the published version.",
          "migration_incomplete": "The published version of **{blueprint_name}** was installed, but at least one reference to `{path}` could not be migrated safely. The old file was kept, and the Repair remains open. Update the remaining `use_blueprint.path` reference manually and check for updates again."
        },
        "step": {
          "init": {
            "description": "The local file for **{blueprint_name}** at `{path}` differs from the published version. The integration has not overwritten it and cannot update this blueprint until you choose how to handle the local changes.",
            "menu_options": {
              "ignore": "Keep the local version and ignore this Repair",
              "restore": "Replace it with the published version"
            },
            "title": "Resolve local changes in {blueprint_name}"
          },
          "restore": {
            "description": "The current file at `{path}` will be backed up and replaced with the latest published version of **{blueprint_name}**. Local edits cannot be merged.\n\nIf the file is still located under `hippo/`, existing YAML `use_blueprint.path` references will be migrated to `hippotastic/`. If any reference cannot be migrated safely, the old file will be kept.\n\nSelect **Submit** to confirm the replacement.",
            "title": "Replace local {blueprint_name}"
          }
        }
      },
      "title": "Local changes in {blueprint_name}"
    }
  }
}
